{
  "name": "short-number2",
  "version": "0.0.0",
  "main": "index.js",
  "repository": "git@github.com:FinalDes/short-number2.git",
  "author": "Alex Alonso <alex4551@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "del-cli dist && tsc -p tsconfig.json",
    "lint": "tslint -p tslint.json",
    "test": "jest",
    "test:verbose": "jest --verbose --no-cache",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "coveralls": "^3.0.3",
    "del-cli": "^1.1.0",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.0",
    "ts-node": "^8.0.3",
    "tslint": "^5.14.0",
    "typescript": "^3.3.4000"
  },
  "jest": {
    "verbose": true,
    "preset": "ts-jest",
    "testRegex": "(tests/.*|\\.(test|spec))\\.ts$",
    "moduleFileExtensions": [
      "ts",
      "js",
      "json"
    ]
  }
}
